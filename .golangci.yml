version: "2"
run:
  timeout: 10m
linters:
  default: standard
  enable:
    - errorlint
    - gocritic
    - misspell
    - revive
    - unconvert
  exclusions:
    paths:
      - bindings
formatters:
  enable:
    - gofmt
    - goimports
